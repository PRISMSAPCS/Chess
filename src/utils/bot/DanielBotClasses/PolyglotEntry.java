package utils.bot.DanielBotClasses;

// an entry from the polyglot opening book
public class PolyglotEntry {
	long key;
	short move;
	short count;
	
	public PolyglotEntry(long Key, short Move, short Count) {
		key = Key;
		move = Move;
		count = Count;
	}
}